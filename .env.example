# AI API Credentials
# Anthropic (Claude) - PRINCIPAL
ANTHROPIC_API_KEY=sk-ant-xxxxx  # Tu API key de Anthropic

# OpenAI credentials (opcional, si usas OpenAI)
OPENAI_API_KEY=sk-your-key-or-google-key
OPENAI_MODEL=gemini-2.5-flash  # or gpt-4o-mini if using OpenAI
OPENAI_TEMPERATURE=0.2
# Optional: OpenAI-compatible base URL (e.g., Gemini)
# OPENAI_API_BASE=https://generativelanguage.googleapis.com/openai/v1
# Optional: alternative envs for Google/Gemini
# GOOGLE_API_KEY=your-google-key
# GOOGLE_API_BASE=https://generativelanguage.googleapis.com/openai/v1
# GEMINI_MODEL=gemini-1.5-flash
# GEMINI_TEMPERATURE=0.2

# Database Configuration
# Set USE_POSTGRES=true to use PostgreSQL instead of SQLite
USE_POSTGRES=false

# PostgreSQL Connection (when USE_POSTGRES=true)
# Desarrollo Local con Docker:
POSTGRES_HOST=postgres  # nombre del servicio en docker-compose
POSTGRES_PORT=5432

# Producción AWS (descomentar y configurar):
# POSTGRES_HOST=your-rds-endpoint.rds.amazonaws.com
# POSTGRES_PORT=5432

POSTGRES_DB=beansco_main
POSTGRES_USER=beansco
POSTGRES_PASSWORD=changeme123  # ⚠️ CAMBIAR EN PRODUCCIÓN
POSTGRES_SCHEMA=public  # For multi-tenant: use tenant_<phone>

# Legacy Supabase (if you were using it before)
# SUPABASE_DB_HOST=your-project.supabase.co
# SUPABASE_DB_PORT=5432
# SUPABASE_DB_NAME=postgres
# SUPABASE_DB_USER=postgres
# SUPABASE_DB_PASSWORD=your-db-password

# Baileys WhatsApp Connector
BAILEYS_SESSION_DIR=/app/.baileys_auth
BAILEYS_LOG_LEVEL=info
BAILEYS_RECONNECT_DELAY_MS=5000
BAILEYS_AUTO_CREATE_TENANT=false
BAILEYS_DEFAULT_CURRENCY=USD
BAILEYS_DEFAULT_LANGUAGE=es

# Redis Cache Configuration
REDIS_ENABLED=false
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# Backend URL (para WhatsApp client en Docker)
BACKEND_URL=http://backend:8000

# PgAdmin (opcional, solo desarrollo con --profile tools)
PGADMIN_PASSWORD=admin123

# AWS Configuration (solo si despliegas a AWS con deploy.sh)
# AWS_REGION=us-east-1
# AWS_ACCOUNT_ID=123456789012
